import React from 'react';

const LoginInput = ({ children }) => {
  return (
    <div className="mt-16 flex h-screen w-full flex-1 flex-col px-4">
        {children}
    </div>
  );
};

export default LoginInput;
